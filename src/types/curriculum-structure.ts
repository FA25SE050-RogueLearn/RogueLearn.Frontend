// Minimal type stubs to satisfy build for curriculum structure API.
// Replace with precise interfaces if/when backend contracts are finalized.

export type CurriculumStructureDto = {
  id?: string;
  [key: string]: any;
};

export type AddSubjectToCurriculumCommandRequest = {
  [key: string]: any;
};

export type AddSubjectToCurriculumResponse = {
  [key: string]: any;
};

export type UpdateCurriculumStructureCommandRequest = {
  id: string;
  [key: string]: any;
};

export type UpdateCurriculumStructureResponse = {
  [key: string]: any;
};