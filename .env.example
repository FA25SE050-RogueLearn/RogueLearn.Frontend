# roguelearn-web/.env.example
# Supabase connection details required for authentication and database access
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# URL for the consolidated core backend service (RogueLearn.User)
# This single endpoint now handles User, Quest, Social, and Meeting domains.
NEXT_PUBLIC_API_URL=https://api.roguelearn.com/v1

# URL for the separate Code Battle backend service (RogueLearn.EventService)
NEXT_PUBLIC_CODE_BATTLE_API_URL=https://codebattle.api.roguelearn.com/v1

# Google Identity Services OAuth Client ID for Meet API integration
# Obtain from Google Cloud Console (OAuth 2.0 Client IDs) and ensure the Meet API is enabled.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-oauth-client-id

# Liveblocks public key for real-time collaboration
NEXT_LIVE_BLOCKS_PUBLIC_KEY=your-liveblocks-public-key

# Google API key for Meet API integration
NEXT_PUBLIC_GOOGLE_API_KEY=your-google-api-key
